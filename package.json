{
  "name": "linkedin-leadgen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone cross-platform CLI for LinkedIn lead-gen & outreach — agent-driven ICP, browser transport, self-pacing rate limits, results to files.",
  "bin": {
    "lk": "bin/lk.mjs"
  },
  "scripts": {
    "lk": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "patchright": "^1.49.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
