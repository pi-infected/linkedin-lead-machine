#!/usr/bin/env sh
# Shim POSIX (Linux/macOS) : délègue au lanceur Node cross-platform bin/lk.mjs.
# Sur Windows, utilise la commande `lk` installée par npm, ou `node bin\lk.mjs`.
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec node "$ROOT/lk.mjs" "$@"
